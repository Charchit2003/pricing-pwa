import type { MaterialDefinition } from "../types/db1";
export function MaterialSelector({materials,value,onChange}:{materials:MaterialDefinition[];value:string;onChange:(id:string)=>void}){return <div className="form-field"><label>Material</label><select value={value} onChange={e=>onChange(e.target.value)}><option value="">Select material</option>{materials.map(m=><option key={m.materialId} value={m.materialId}>{m.materialName}</option>)}</select></div>}
